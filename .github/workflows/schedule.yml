name: 10 Minutes check Schedule

on:
  schedule:
    - cron: '*/10 * * * *'

jobs:
  hourly-check:
    runs-on: ubuntu-latest
    steps:
      - name: 10 Minutes check
        run: echo "✅ 10 Minutes check"



