name: star repo

on: 
  watch:
    types: [started]

jobs:
  log-someone-star:
    runs-on:  ubuntu-latest
    steps:
      - name: Log user
        run: echo "User ${{ github.actor }} just starred your repository!"
